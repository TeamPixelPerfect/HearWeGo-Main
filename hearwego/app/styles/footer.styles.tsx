"use client";
import { styled } from "@mui/material";

export const FooterContainer = styled("div")(({ theme }) => ({
    width: "100%",  
    height: "auto",
    background: "#4338CA",
    color: "#fff",
    marginTop: "auto",  
    "a" : {
        color: "#fff",
    }
}));